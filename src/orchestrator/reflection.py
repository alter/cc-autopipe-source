#!/usr/bin/env python3
"""orchestrator.reflection — META_REFLECT mechanism.

Placeholder populated by GROUP H (PROMPT_v1.3-FULL.md). Public surface
will include:
  - write_meta_reflect(project_dir, task_id, stage, failures, ...)
  - read_meta_decision(project_dir, target_md_path)
  - apply_meta_decision(project_dir, decision, task_id)
"""

from __future__ import annotations
